import React from 'react';

enum SubscriptionProcessStatus {
  IDLE = 'IDLE',
  SIGNING = 'SIGNING',
  VERIFYING = 'VERIFYING',
  SUCCESS = 'SUCCESS',
  FAILED = 'FAILED',
}

const useSigner = () => {
  return {};
};

export default useSigner;
